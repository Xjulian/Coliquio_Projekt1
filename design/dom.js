function dom(){
  var ergaenzung = document.getElementById("liste1").childNodes[0];
  var ergaenzung_erweitert = ergaenzung.nodeValue + " 1";
  ergaenzung.nodeValue = ergaenzung_erweitert;
  var ergaenzung = document.getElementById("liste2").childNodes[0];
  var ergaenzung_erweitert = ergaenzung.nodeValue + " 2";
  ergaenzung.nodeValue = ergaenzung_erweitert;
  var ergaenzung = document.getElementById("liste3").childNodes[0];
  var ergaenzung_erweitert = ergaenzung.nodeValue + " 3";
  ergaenzung.nodeValue = ergaenzung_erweitert;
  var ergaenzung = document.getElementById("liste4").childNodes[0];
  var ergaenzung_erweitert = ergaenzung.nodeValue + " 4";
  ergaenzung.nodeValue = ergaenzung_erweitert;
//  if(document.getElementById('button()').onclick == true){
//  alert("adhgsgl");}
//  Funktioniert nicht!
}
